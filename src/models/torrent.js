export default class Torrent {
    constructor(name, subname, file_id, desc, category, imdb_link, nfo_id) {
        this.name = name
        this.subname = subname
        this.file_id = file_id
        this.desc = desc
        this.category = category
        this.imdb_link = imdb_link
        this.nfo_id = nfo_id
    }
}
